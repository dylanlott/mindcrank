[package]
name = "mindcrank"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "A small, extensible library for composing and running Monte Carlo simulations of Magic: The Gathering decks"
license = "MIT OR Apache-2.0"

[dependencies]
rand = "0.10.2"
rayon = "1.12.0"

[dev-dependencies]
approx = "0.5.1"
